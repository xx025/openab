# OpenAB 配置文件示例（YAML）
# 复制到默认路径：~/.config/openab/config.yaml（或 config.yml / config.json）
# 或设置环境变量 OPENAB_CONFIG 指向本文件。
# 不设置 workspace 时默认使用用户家目录（~）。

# 用户级 systemd 服务（openab install-service）及「openab run」无子命令时，仅从此项解析运行目标：
# 不设置或无效时默认为 serve
# service:
#   run: telegram   # serve | telegram | discord

agent:
  backend: cursor   # cursor | agent（与 cursor 等价，Cursor CLI 名为 agent）| codex（已实现）；gemini | claude | openclaw 尚未实现
  # workspace: ~   # 不填或 ~ 表示家目录
  timeout: 300

telegram:
  bot_token: ""     # 从 @BotFather 获取
  allowed_user_ids: []  # 用户发送 /whoami 可查看 ID

discord:
  bot_token: ""     # 从 Discord 开发者门户获取
  allowed_user_ids: []

# OpenAI API 兼容服务（openab serve）
# api:
#   host: "127.0.0.1"   # 监听地址，默认 127.0.0.1
#   port: 8000          # 监听端口，默认 8000
#   key: ""             # 可选：设置后请求需带 Authorization: Bearer <key>

# 以下为各后端可选，多数情况可不写
# cursor:
#   cmd: agent
#   continue_session: true   # 使用上一会话（agent --continue），默认 true
# openclaw:   # 需先安装 npm i -g openclaw 并运行 openclaw gateway
#   cmd: openclaw
#   thinking: off   # off | minimal | low | medium | high | xhigh
# codex:
#   cmd: codex
#   skip_git_check: true    # 默认 true，允许非 Git 目录执行
#   continue_session: true # 默认延续上一会话（exec resume --last）
# gemini:
#   cmd: gemini
# claude:
#   cmd: claude
#   model: sonnet
#   max_turns: 10
#   add_dir: /path/to/extra
