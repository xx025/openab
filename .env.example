# Telegram 机器人 Token（在 @BotFather 创建机器人后获得）
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# 智能体后端：cursor（默认）、codex、gemini、claude
# OPENAB_AGENT=cursor

# 可选：工作目录（Cursor/Codex 共用），不设则使用当前目录
# OPENAB_WORKSPACE=/path/to/workspace
# CURSOR_WORKSPACE=/path/to/workspace

# 可选：单次执行超时（秒），默认 300
# OPENAB_AGENT_TIMEOUT=300
# CURSOR_AGENT_TIMEOUT=300

# --- Cursor ---
# 可选：Cursor agent 可执行文件，默认 PATH 中的 agent
# CURSOR_AGENT_CMD=agent

# --- Codex (https://github.com/openai/codex) ---
# CODEX_CMD=codex
# CODEX_SKIP_GIT_CHECK=0
# 认证：codex login 或 CODEX_API_KEY

# --- Gemini CLI (https://github.com/google-gemini/gemini-cli) ---
# 可选：gemini 可执行文件，默认 PATH 中的 gemini（npm i -g @google/gemini-cli 或 brew install gemini-cli）
# GEMINI_CLI_CMD=gemini
# 认证：运行 gemini 登录，或设置 GEMINI_API_KEY

# --- Claude CLI ---
# 可选：Claude CLI 可执行文件，默认 PATH 中的 claude；调用方式为 claude -p "prompt"
# CLAUDE_CLI_CMD=claude
# 认证：由所用 Claude CLI 自行管理（如 ANTHROPIC_API_KEY）

# 鉴权：只允许以下 Telegram 用户 ID 使用（逗号分隔）。不设则无人可使用。
# 用户可发送 /whoami 查看自己的 User ID，由管理员加入此处后即可使用。
# ALLOWED_USER_IDS=123456789,987654321
